import React from 'react'
import styled from 'styled-components'

export const StyledGradient = styled.div`
  flex-grow: 0;
  flex-shrink: 0;
  touch-action: none;
  background: linear-gradient(180deg, var(--bgcolor1), var(--bgcolor2));
`